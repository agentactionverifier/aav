# Basic Local HTTP

Run `aav init`, export the one-time `AAV_AUTH_TOKEN`, then run `aav start`. The authenticated local observation endpoint is `http://127.0.0.1:7331/v1/agent/me`. MCP actions and HTTP tools share the same Local AAV enforcement pipeline.
